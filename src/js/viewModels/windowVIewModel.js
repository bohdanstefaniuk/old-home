// Open window (generate view, render window content, add to task bar, set as active window)
// Close window (destroy view, delete from task bar)

function WindowViewModel(contentViewModel) {
	





}